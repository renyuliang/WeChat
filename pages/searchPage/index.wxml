<!--pages/searchPage/index.wxml-->
<view class="container-input" wx:if="{{!searching}}">
<!-- 模拟输入框 -->
  <view class="demo-input" bindtap="showSearch">点击输入框</view>
</view>
<v-search wx:if="{{searching}}" bind:cancel="hideSearch"/>